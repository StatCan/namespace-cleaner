---
apiVersion: v1
kind: ConfigMap
metadata:
  name: namespace-cleaner-test-users
data:
  users: "test-user@company.com,restored-user@example.org"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: namespace-cleaner-config
data:
  TEST_MODE: "true"
  DRY_RUN: "true"
  ALLOWED_DOMAINS: "example.com,test.com"
  TEST_USERS: "test1@example.com,test2@example.com"
  GRACE_PERIOD: "30"
  # These can be dummy values in test mode
  CLIENT_ID: "dummy-client-id"
  CLIENT_SECRET: "dummy-secret"
  TENANT_ID: "dummy-tenant"
