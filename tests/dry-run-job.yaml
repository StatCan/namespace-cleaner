apiVersion: batch/v1
kind: Job
metadata:
  name: namespace-cleaner-dry-run
  namespace: das
  labels:
    app: namespace-cleaner
spec:
  ttlSecondsAfterFinished: 60
  template:
    metadata:
      labels:
        app: namespace-cleaner
    spec:
      serviceAccountName: namespace-cleaner
      containers:
        - name: cleaner
          image: artifactory.cloud.statcan.ca/das-aaw-docker/namespace-cleaner:a3a53190abad15a731b8fbe1ff35268151d38d7d
          command: ["/namespace-cleaner", "-dry-run"]
          envFrom:
            - secretRef:
                name: microsoft-graph-api-secret
            - configMapRef:
                name: namespace-cleaner-config
          env:
            - name: DRY_RUN
              value: "true"
            - name: TEST_MODE
              value: "false"
      restartPolicy: Never
