---
apiVersion: batch/v1
kind: Job
metadata:
  name: namespace-cleaner-container-job
  namespace: das
spec:
  template:
    spec:
      serviceAccountName: namespace-cleaner
      containers:
        - name: namespace-cleaner-container
          image: artifactory.cloud.statcan.ca/das-aaw-docker/namespace-cleaner:384a9fb39c94ccf8959f8a037eac59dd7fae90e9
          envFrom:
            - secretRef:
                name: microsoft-graph-api-secret
            - configMapRef:
                name: namespace-cleaner-dry-run-config
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
      restartPolicy: Never
